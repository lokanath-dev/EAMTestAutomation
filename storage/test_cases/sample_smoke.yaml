name: sample_smoke
description: Basic scaffold smoke case
tags:
  - smoke
steps:
  - step_name: Open EAM
    action_name: Open Enrollment Application Manager
    parameters:
      target: home
  - step_name: Validate GraphQL
    action_name: Validate Backend State via GraphQL
    parameters:
      query_name: member_status
datasets:
  - sample_dataset
